<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Modularny Monolit{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🛒</text></svg>">
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body>
        <nav class="navbar">
            <div class="navbar-container">
                <a href="{{ path('catalog_product_index') }}" class="navbar-brand">🏪 ModuShop</a>
                <ul class="navbar-nav">
                    <li><a href="{{ path('catalog_product_index') }}">Produkty</a></li>
                    <li><a href="{{ path('catalog_category_index') }}">Kategorie</a></li>
                    <li><a href="{{ path('inventory_stock_index') }}">Magazyn</a></li>
                    <li><a href="{{ path('cart_index') }}" class="cart-link">🛒 Koszyk</a></li>
                </ul>
            </div>
        </nav>

        <main class="container">
            {% block body %}{% endblock %}
        </main>
    </body>
</html>
